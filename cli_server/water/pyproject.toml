[project]
name = "water"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["robot-common"]

[project.scripts]
lower-cup = "water:lower_cup_main"
deliver-cup = "water:deliver_cup_main"
retract-cup = "water:retract_cup_main"
