# 异步 HTTP / WebSocket
aiohttp>=3.14
httpx>=0.28
websockets>=16.0
websocket-client>=1.9

# LLM / Agent
openai>=2.44
langgraph>=1.2
langgraph-checkpoint>=4.1
langgraph-prebuilt>=1.1

# 音频
pyaudio>=0.2
sounddevice>=0.5
soundfile>=0.14
scipy>=1.13.0,<1.18.0

# 视频
opencv-python>=4.9.0,<5.0

# 唤醒词
sherpa-onnx>=1.13

# 本地 ASR（sherpa-onnx，ASR_PROVIDER=sherpa_onnx 时需要）
# 注：sherpa-onnx 已在唤醒词依赖中声明
modelscope>=1.38
torch>=2.13

# ROS2 消息解析
rosbags>=0.11

# 拼音（同音字过滤）
pypinyin>=0.55

# 工具
numpy>=1.26.0,<2.0.0
pyyaml>=6.0
python-dotenv>=1.2
Pillow>=12.3